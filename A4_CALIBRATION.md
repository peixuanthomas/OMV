# OpenMV 红色 A4 标定

## 坐标定义

- 红色 A4 长边向右，短边向下。
- 左上角为原点 O。
- X 轴向下，范围 `0–210 mm`。
- Y 轴向右，范围 `0–297 mm`。

## 使用顺序

1. 在 OpenMV IDE 中运行 `fov_preview.py`。
   - 调整焦点并确认整张 A4 均在画面内。
   - A4 四边建议距离画面边缘至少 12 px。
2. 停止预览，运行 `a4_calibration.py`。
   - 标定时移除其他大面积红色物体。
   - 绿色粗线是检测到的 A4 外框。
   - 黄色圆圈依次标出 `TL O / TR / BR / BL`。
   - 纸张必须保持长边向右、短边向下并全程静止。
3. 程序自动搜索镜头参数并采集 60 个稳定帧。
   - `EDGE max/rms` 是当前边缘直线残差。
   - `MARGIN` 是 A4 与画面边缘的最小距离。
   - 估计最大误差大于 1 mm 时不会覆盖旧标定。
4. 出现 `CALIBRATION_SAVED` 后停止脚本，重新运行 `main.py`。

成功标定保存在设备根目录的 `a4_calibration.json`。文件只保存镜头固有
畸变参数，**不会保存 A4 四角或坐标原点**。正式程序每帧先去除镜头畸变，
再从当前灰度画面动态检测 A4 四角、展开 A4 平面，最后进行多边形识别。
因此相机相对底面轻微左右移动后，坐标原点仍会跟随当前 A4 左上角更新。

## 串口数据

现有 `vertices_px` 保持为展开画面的像素坐标。标定有效时，每个多边形还会
输出：

- `vertices_xy_mm`：每个点是 `[X向下, Y向右]`；
- `centroid_xy_mm`；
- `edge_lengths_mm`。

如果标定文件缺失、质量不合格或相机方向发生变化，串口会报告
`calibration_required`，且不会输出毫米坐标。

## 精度边界

1 mm 指纯色 A4 四边直线残差、角点平均稳定性和像素量化共同得到的模型估计
上限。纯色纸内部没有独立真值点，因此不能将其解释为所有内部点都经过物理
实测。被测目标还必须与 A4 位于同一平面；物体厚度会产生额外视差。
